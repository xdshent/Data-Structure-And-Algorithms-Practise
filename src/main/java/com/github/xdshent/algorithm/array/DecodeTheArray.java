package com.github.xdshent.algorithm.array;

/**
 * Decode the array constructed from another array
 * Given an array constructed from another array
 * by taking sum of every distinct pair in it,
 * decode the array to get back the original array
 * elements.
 */
public class DecodeTheArray {
}
